[project]
name = "safetea"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "dotenv>=0.9.9",
    "pytest>=9.0.3",
    "web3>=7.0.0",
]

[tool.setuptools.package-data]
safetea = ["abis/*.json"]

[tool.pytest.ini_options]
pythonpath = [
  "."
]
